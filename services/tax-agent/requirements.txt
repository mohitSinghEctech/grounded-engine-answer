# Direct dependencies. Run `pip freeze > requirements.txt` after the first
# install to lock transitives, matching the llm-gateway's fully-pinned style.
fastapi==0.141.1
httpx==0.28.1
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
uvicorn==0.52.4
